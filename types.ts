export interface NavItem {
  label: string;
  href: string;
}

export interface ProjectStats {
  cost: string;
  sqFt: string;
  acres: number;
}

export interface Partner {
  name: string;
  role: string;
  responsibility: string;
}